require 'spec_helper'

describe ItemsController do
  
  context "when a user isn't a member of a project" do
    
    it "doesn't let them create items"
    
    it "doesn't let them edit items"
    
  end
end